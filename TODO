If 0 commits, show a 'plz commit' page

If non existant ?branch=pouet redirect_to the /aq_rep/<n>

Improve display of private path, not only owner but for commiters too!

Improve commit listing, commit display

Colorize files view and commit diff display

Check for all non-escapped params[] in templates
