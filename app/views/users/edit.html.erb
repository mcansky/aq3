<% content_for :head do %>
  <title><%= Settings.application.name %> - <%= t(:user_edit) %></title>
<% end %>

<h1><%= t(:user_edit) %> <%= @user.login %></h1>

<%= render "form", :user => @user %>

<%= link_to t(:user_list), users_path %>
