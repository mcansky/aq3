<% for repo in repositories %>
	<div class="repository">
		<h3>
			<%= link_to repo.owner.login + "/" + repo.name, repo %>
			<% for user in repo.users %>
				<% if user == repo.owner %>
					<% if user == current_user %>
						<%= image_tag("/images/icons/user-worker-boss.png", :title => t(:owner), :alt => t(:owner), :border => 0)%>
					<% else %>
						<%= link_to image_tag("/images/icons/user-worker-boss.png", :title => t(:owner), :alt => t(:owner), :border => 0), user %>
					<% end %>
				<% end %>
				<% if repo.committers.include?(user) %>
					<%= image_tag("/images/icons/user-worker.png", :title => t(:committer), :alt => t(:committer), :border => 0)%>
				<% end %>
			<% end %>
		</h3>
		<div class="desc">
			<span class="img"><%= image_tag("/images/icons/big/drives_11.png", :border => 0) %></span>
			<p>blah</p>
			<div class="clear"></div>
		</div>
	</div>
<% end %>