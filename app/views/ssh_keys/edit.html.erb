<% content_for :head do %>
  <title><%= Settings.application.name %> - <%= t(:sshkey_edit) %></title>
<% end %>

<div id="sshkeys">
<h2><%= t(:sshkey_edit) %></h2>

<%= render "form", :key => @key %>

<%= link_to t(:sshkeys_list), ssh_keys_path %> | <%= link_to t(:cancel), @key %>
</div>
