<% content_for :head do %>
  <title><%= Settings.application.name %> - <%= t(:sshkey_new) %></title>
<% end %>

<h1><%= t(:sshkey_new) %></h1>

<%= render "form", :key => @key %>

<%= link_to t(:sshkeys_list), ssh_keys_path %>
