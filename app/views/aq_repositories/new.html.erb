<h1><%= t(:repo_new) %></h1>

<% form_for @repository do |f| %>
	<%= f.error_messages %>
	<p><%= t(:name) %> : <%= f.text_field :name %></p>
	<p><%= f.submit t(:submit) %></p>
<% end %>

<%= link_to t(:repo_list), :controller => "aq_repositories", :action => "index" %>